name: "My Action"
description: "My custom action"
author: "Angel Roman"

run:
  using: "composite"
  steps:
    - name: Step 1
      id: step_id
      shell: bash
      run: |
        echo "Hello World"

branding:
  icon: "terminal"
  color: "blue"

inputs:
  my_input:
    description: "My primer mennsaje"
    required: true
    default: "Hello World"
  my_input2:
    description: "My segundo mennsaje
    required: false
    default: "Hello World 2"

outputs:
  my_output:
    description: "My output message"
    value: ${{ imputs.my_input }}
  my_output2:
    description: "My output message 2"
    value: ${{ steps.step_id.outputs.my_input }}


